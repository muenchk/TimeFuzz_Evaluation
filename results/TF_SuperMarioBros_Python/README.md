## Deleted Files

All exported files with the ending dump.txt have been deleted as they are not used in this case. The files needed to run the inputs are the cmdargs.txt and scriptargs.txt files

Additionally, the columns ´´´Primary Score´´´ and ´´´Secondary Score´´´ have been removed from the csv files to save on space. They can be obtanined once again by running the corresponding inputs through the oracle.